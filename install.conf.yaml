- defaults:
    link:
      relink: true
    shell:
      stdin: true
      stdout: true
      stderr: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zprofile:
    ~/.gitconfig:

- create:
  - ~/work

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
  - [./setup_homebrew.zsh, Setting up Homebrew]
