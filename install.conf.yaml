- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zprofile:
    ~/.gitconfig:

- create:
  - ~/work

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
